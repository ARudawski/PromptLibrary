# Unit Tests

Unit tests start in Slice 1.

Use TDD for deterministic core behavior:

- parser;
- validation;
- alias/index rules;
- projection;
- use cases;
- prompt source adapters with mocked I/O;
- cache behavior.
