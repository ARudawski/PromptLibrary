# Test Fixtures

Fixture folders planned for Slice 1:

```text
prompts-valid/
prompts-invalid/
prompts-conflicts/
```

Minimum Slice 1 fixture set:

- one valid active prompt;
- one draft prompt;
- one invalid prompt missing a required field;
- one unknown command case;
- one alias case;
- one alias conflict case.
